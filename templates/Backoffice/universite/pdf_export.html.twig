x<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Establishments Export - {{ universite.nom }}</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #2c3e50; text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #3498db; color: white; padding: 10px; text-align: left; }
        td { padding: 8px; border-bottom: 1px solid #ddd; }
        .footer { margin-top: 30px; text-align: right; font-size: 12px; color: #7f8c8d; }
    </style>
</head>
<body>
    <h1>Establishments List - {{ universite.nom }}</h1>
    <p>Generated on: {{ date|date('Y-m-d H:i') }}</p>
    
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Type</th>
                <th>Location</th>
                <th>Website</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            {% for etablissement in establishments %}
            <tr>
                <td>{{ etablissement.nom }}</td>
                <td>{{ etablissement.etype }}</td>
                <td>{{ etablissement.localisation }}</td>
                <td>{{ etablissement.siteweb ?? 'N/A' }}</td>
                <td>{{ etablissement.email }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        Total: {{ establishments|length }} establishments
    </div>
</body>
</html>