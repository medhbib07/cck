```html
<!DOCTYPE html>
<html lang="fr">
<head>
    <!-- ========== Meta Tags ========== -->
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="modinatheme">
    <meta name="description" content="CCK - Contacter {{ etablissement.nom }} pour plus d'informations sur les programmes et admissions">
    <!-- ======== Page title ============ -->
    <title>Contacter {{ etablissement.nom }} - CCK</title>
    <!--<< Favicon >>-->
    <link rel="shortcut icon" href="assets/img/favicon.svg">
    <!--<< Bootstrap min.css >>-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <!--<< Font Awesome.css >>-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <!--<< Animate.css >>-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <!--<< Google Fonts >>-->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap">
    <style>
        :root {
            --prussian-blue: #25354A;
            --sky-blue: #6CC1D5;
            --tufts-blue: #498ED3;
            --glaucous: #5C79A5;
            --platinum: #E8ECE9;
            --error-red: #dc3545;
            --success-green: #28a745;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: var(--prussian-blue);
            line-height: 1.6;
        }

        /* Header */
        .site-header {
            background: linear-gradient(90deg, var(--prussian-blue) 0%, var(--tufts-blue) 100%);
            color: white;
            padding: 1.5rem 0;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .site-header .logo img {
            max-height: 60px;
        }

        .site-header .nav-link {
            color: white;
            font-weight: 500;
            margin-left: 1.5rem;
            transition: color 0.3s;
        }

        .site-header .nav-link:hover {
            color: var(--sky-blue);
        }

        /* Contact Section */
        .contact-section {
            background: white;
            border-radius: 16px;
            box-shadow: 0 12px 24px rgba(0,0,0,0.1);
            padding: 3rem;
            margin: 3rem auto;
            max-width: 900px;
            animation: fadeInUp 0.6s ease-out;
        }

        .establishment-header {
            background: linear-gradient(45deg, var(--sky-blue), var(--tufts-blue));
            color: white;
            padding: 2rem;
            border-radius: 12px 12px 0 0;
            margin-bottom: 2rem;
            text-align: center;
        }

        .establishment-header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin: 0;
        }

        .establishment-info {
            display: flex;
            align-items: center;
            margin-bottom: 2rem;
            background: var(--platinum);
            padding: 1.5rem;
            border-radius: 10px;
        }

        .establishment-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            overflow: hidden;
            margin-right: 1.5rem;
            border: 3px solid var(--sky-blue);
        }

        .establishment-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .establishment-details h2 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .establishment-details p {
            margin: 0;
            color: var(--glaucous);
        }

        /* Form Styling */
        .form-label {
            font-weight: 600;
            color: var(--prussian-blue);
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }

        .form-control, .form-select {
            border: 2px solid #e0e4e8;
            border-radius: 10px;
            padding: 0.75rem 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--sky-blue);
            box-shadow: 0 0 8px rgba(108, 193, 213, 0.3);
            outline: none;
        }

        .form-control.is-invalid, .form-select.is-invalid {
            border-color: var(--error-red);
            background-image: none;
        }

        .form-error {
            color: var(--error-red);
            font-size: 0.85rem;
            margin-top: 0.25rem;
            background: #f8d7da;
            padding: 0.25rem 0.5rem;
            border-radius: 5px;
        }

        .btn-submit {
            background: var(--sky-blue);
            color: white;
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .btn-submit:hover {
            background: var(--tufts-blue);
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }

        .back-link {
            color: var(--glaucous);
            text-decoration: none;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            margin-top: 1.5rem;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: var(--prussian-blue);
        }

        /* Alerts */
        .alert {
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 1.5rem;
            font-size: 0.95rem;
        }

        .alert-success {
            background: #e6f4ea;
            color: var(--success-green);
            border: 1px solid var(--success-green);
        }

        .alert-error {
            background: #f8d7da;
            color: var(--error-red);
            border: 1px solid var(--error-red);
        }

        /* Footer */
        .site-footer {
            background: var(--prussian-blue);
            color: white;
            padding: 3rem 0;
            margin-top: 3rem;
        }

        .site-footer a {
            color: var(--sky-blue);
            text-decoration: none;
            transition: color 0.3s;
        }

        .site-footer a:hover {
            color: white;
        }

        .footer-logo img {
            max-height: 80px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .contact-section {
                padding: 2rem;
                margin: 2rem 1rem;
            }

            .establishment-header h1 {
                font-size: 1.5rem;
            }

            .establishment-info {
                flex-direction: column;
                text-align: center;
            }

            .establishment-image {
                margin-right: 0;
                margin-bottom: 1rem;
            }

            .form-control, .form-select {
                font-size: 0.95rem;
            }
        }

        @media (max-width: 576px) {
            .btn-submit {
                width: 100%;
                padding: 0.75rem;
            }

            .back-link {
                display: block;
                text-align: center;
            }
        }
    </style>
</head>
<body>
  

    <!-- Contact Section -->
    <div class="container py-5">
        <div class="contact-section animate__animated animate__fadeInUp">
            <div class="establishment-header">
                <h1>Contacter {{ etablissement.nom }}</h1>
            </div>
            
            <div class="establishment-info">
                <div class="establishment-image">
                    {% if etablissement.logo %}
                        <img src="{{ asset('Uploads/establishments/' ~ etablissement.logo) }}" alt="{{ etablissement.nom }}" loading="lazy">
                    {% else %}
                        <img src="{{ asset('img/default-establishment.jpg') }}" alt="Default establishment image" loading="lazy">
                    {% endif %}
                </div>
                <div class="establishment-details">
                    <h2>{{ etablissement.nom }}</h2>
                    <p>{{ etablissement.groupe ? etablissement.groupe.nom : 'Non affilié' }}</p>
                    <p class="text-muted">{{ etablissement.ville }}</p>
                </div>
            </div>

            {% for message in app.flashes('success') %}
                <div class="alert alert-success animate__animated animate__fadeIn">
                    <i class="fas fa-check-circle me-2"></i>{{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-error animate__animated animate__fadeIn">
                    <i class="fas fa-exclamation-circle me-2"></i>{{ message }}
                </div>
            {% endfor %}

            <form action="{{ path('app_contact_etablissement', {'id': etablissement.id}) }}" method="post" novalidate>
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('contact_etablissement') }}">
                <div class="row">
                    <div class="col-md-6 mb-4">
                        <label for="contact_etablissement_nom" class="form-label">Votre nom complet</label>
                        <input type="text" id="contact_etablissement_nom" name="contact_etablissement[nom]" class="form-control" placeholder="Nom et prénom" value="{{ form_data.nom is defined ? form_data.nom : '' }}" required aria-required="true">
                        {% for error in form_errors %}
                            {% if error.getPropertyPath() == '[nom]' %}
                                <div class="form-error">{{ error.getMessage() }}</div>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <div class="col-md-6 mb-4">
                        <label for="contact_etablissement_email" class="form-label">Votre email</label>
                        <input type="email" id="contact_etablissement_email" name="contact_etablissement[email]" class="form-control" placeholder="email@exemple.com" value="{{ form_data.email is defined ? form_data.email : '' }}" required aria-required="true">
                        {% for error in form_errors %}
                            {% if error.getPropertyPath() == '[email]' %}
                                <div class="form-error">{{ error.getMessage() }}</div>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-md-6 mb-4">
                        <label for="contact_etablissement_telephone" class="form-label">Votre téléphone</label>
                        <input type="tel" id="contact_etablissement_telephone" name="contact_etablissement[telephone]" class="form-control" placeholder="+216 XX XXX XXX" value="{{ form_data.telephone is defined ? form_data.telephone : '' }}" aria-describedby="telephoneHelp">
                        <small id="telephoneHelp" class="form-text text-muted">Optionnel</small>
                        {% for error in form_errors %}
                            {% if error.getPropertyPath() == '[telephone]' %}
                                <div class="form-error">{{ error.getMessage() }}</div>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <div class="col-md-6 mb-4">
                        <label for="contact_etablissement_niveau_etude" class="form-label">Votre niveau d'étude</label>
                        <select id="contact_etablissement_niveau_etude" name="contact_etablissement[niveau_etude]" class="form-select" required aria-required="true">
                            <option value="" disabled {% if not form_data.niveau_etude is defined or form_data.niveau_etude == '' %}selected{% endif %}>Sélectionnez un niveau</option>
                            <option value="bac" {% if form_data.niveau_etude is defined and form_data.niveau_etude == 'bac' %}selected{% endif %}>Baccalauréat</option>
                            <option value="licence" {% if form_data.niveau_etude is defined and form_data.niveau_etude == 'licence' %}selected{% endif %}>Licence</option>
                            <option value="master" {% if form_data.niveau_etude is defined and form_data.niveau_etude == 'master' %}selected{% endif %}>Master</option>
                            <option value="doctorat" {% if form_data.niveau_etude is defined and form_data.niveau_etude == 'doctorat' %}selected{% endif %}>Doctorat</option>
                            <option value="autre" {% if form_data.niveau_etude is defined and form_data.niveau_etude == 'autre' %}selected{% endif %}>Autre</option>
                        </select>
                        {% for error in form_errors %}
                            {% if error.getPropertyPath() == '[niveau_etude]' %}
                                <div class="form-error">{{ error.getMessage() }}</div>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
                
                <div class="mb-4">
                    <label for="contact_etablissement_message" class="form-label">Votre message</label>
                    <textarea id="contact_etablissement_message" name="contact_etablissement[message]" class="form-control" placeholder="Décrivez votre demande..." rows="5" required aria-required="true">{{ form_data.message is defined ? form_data.message : '' }}</textarea>
                    {% for error in form_errors %}
                        {% if error.getPropertyPath() == '[message]' %}
                            <div class="form-error">{{ error.getMessage() }}</div>
                        {% endif %}
                    {% endfor %}
                </div>
                
                <button type="submit" class="btn btn-submit">
                    <i class="fas fa-paper-plane me-2"></i> Envoyer le message
                </button>
                
                <a href="{{ path('app_view_universities') }}" class="back-link">
                    <i class="fas fa-arrow-left me-2"></i> Retour aux établissements
                </a>
            </form>
        </div>
    </div>

    <!-- Footer -->
    <footer class="site-footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4">
                    <div class="footer-logo">
                        <img src="https://touritage.rnu.tn/wp-content/uploads/2023/09/logo_cck-mesrs_tunisie_800-768x540-1.png" alt="CCK Logo">
                    </div>
                    <p class="mt-3">Plateforme officielle de gestion des établissements universitaires tunisiens.</p>
                </div>
                <div class="col-md-4 mb-4">
                    <h5 class="text-white mb-3">Liens utiles</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_home') }}">Accueil</a></li>
                        <li><a href="{{ path('app_view_universities') }}">Universités</a></li>
                        <li><a href="#">À propos</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-4">
                    <h5 class="text-white mb-3">Contactez-nous</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-map-marker-alt me-2"></i> Avenue Mohammed V, Tunis, Tunisie</li>
                        <li><i class="fas fa-phone me-2"></i> <a href="tel:+21670000000">+216 70 000 000</a></li>
                        <li><i class="fas fa-envelope me-2"></i> <a href="mailto:contact@cck.tn">contact@cck.tn</a></li>
                    </ul>
                </div>
            </div>
            <div class="text-center mt-4">
                <p class="mb-0">© 2025 <strong>CCK</strong> - Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <!--<< All JS Plugins >>-->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery.nice-select@1.1.0/js/jquery.nice-select.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/odometer@0.4.8/odometer.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery.appear@0.4.1/jquery.appear.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11.1.14/swiper-bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/meanmenu@2.0.8/jquery.meanmenu.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/magnific-popup@1.1.0/dist/jquery.magnific-popup.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/circle-progress@1.0.0/dist/circle-progress.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/wow.js@1.3.0/dist/wow.min.js"></script>
    <script>
        // Initialize WOW.js for animations
        new WOW().init();

        // Initialize Nice Select
        $(document).ready(function() {
            $('select').niceSelect();
        });

        // Form validation feedback
        document.querySelectorAll('.form-control, .form-select').forEach(input => {
            input.addEventListener('input', function() {
                if (this.checkValidity()) {
                    this.classList.remove('is-invalid');
                    this.classList.add('is-valid');
                } else {
                    this.classList.remove('is-valid');
                    this.classList.add('is-invalid');
                }
            });
        });
    </script>
</body>
</html>
```